import { PlusCircleFilled } from "@ant-design/icons";
import { PlusCircleFilledIconProps } from "./PlusCircleFilledIconProps";

function PlusCircleFilledIcon(props: PlusCircleFilledIconProps) {
  return <PlusCircleFilled {...props} />;
}

export default PlusCircleFilledIcon;
